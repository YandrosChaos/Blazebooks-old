<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.blazebooks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="q">
        <activity android:name=".view.becomepremium.BecomePremiumActivity" />
        <activity android:name=".view.reader.ReaderActivity" />
        <activity android:name=".view.PreconfiguredActivity" />
        <activity android:name=".view.settings.SettingsActivity" />
        <activity android:name=".view.showbook.ShowBookActivity" />
        <activity
            android:name=".view.login.SignInActivity"
            android:theme="@style/LoginPage"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".view.login.LoginActivity"
            android:theme="@style/LoginPage"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".view.search.SearchActivity" />
        <activity
            android:name=".view.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>